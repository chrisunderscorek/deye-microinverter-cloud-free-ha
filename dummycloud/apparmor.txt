#include <tunables/global>

profile deye_dummycloud flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  file,
  signal,

  # HA base image entrypoints and Bashio option loading
  /init rix,
  /run.sh rix,
  /bin/** ix,
  /usr/bin/** ix,
  /package/** ix,
  /command/** ix,
  /etc/s6/** rix,
  /run/{,**} rwk,
  /run/{s6,s6-rc*,service}/** rwix,
  /etc/services.d/** rwix,
  /etc/cont-init.d/** rwix,
  /etc/cont-finish.d/** rwix,
  /usr/lib/bashio/** ix,
  /tmp/** rwk,
  /dev/tty rw,
  /dev/null rw,

  # App configuration written by Home Assistant
  /data/** r,

  # Run the Node.js service under a narrower profile.
  /usr/bin/node cx,

  profile /usr/bin/node flags=(attach_disconnected,mediate_deleted) {
    #include <abstractions/base>
    #include <abstractions/nameservice>

    signal receive,

    # TCP server for the inverter and TCP client connections to MQTT/mqtts.
    network inet stream,
    network inet6 stream,

    # DNS lookups for broker hostnames such as core-mosquitto.
    network inet dgram,
    network inet6 dgram,

    /usr/bin/node rm,
    /app/** r,
    /data/** r,
    /dev/null rw,
    /dev/urandom r,
    /etc/ssl/** r,
    /usr/share/ca-certificates/** r,
    /proc/sys/kernel/random/uuid r,
    /proc/sys/net/core/somaxconn r,
    /tmp/** rwk,
    /run/** rwk,
  }
}
